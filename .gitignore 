# ─────────────────────────────────────
# Terraform-specific ignores
# ─────────────────────────────────────

# Local .terraform directories (holds Terraform working data)
.terraform/

# Terraform state files (can contain sensitive data)
*.tfstate
*.tfstate.*

# Crash logs (auto-generated during errors)
crash.log
crash.*.log

# Variable definition files — often contain secrets
*.tfvars
*.tfvars.json

# Local override files — usually not shared across teams
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan output files (can be large or environment-specific)
*.tfplan

# CLI configuration files
.terraformrc
terraform.rc

# ─────────────────────────────────────
# Kubernetes-specific ignores
# ─────────────────────────────────────

# Kubernetes config files with secrets or generated by tools
.kube/
*.kubeconfig

# Helm files (if you're using Helm for deployments)
charts/
*.tgz

# ─────────────────────────────────────
# General development environment
# ─────────────────────────────────────

# macOS system file
.DS_Store

# VS Code settings
.vscode/

# JetBrains IDE settings
.idea/

# Log and temp files
*.log
*.tmp

